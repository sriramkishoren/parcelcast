__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/
.env

# Raw M5 input data — large, redownloadable from Kaggle
data/sales_train_evaluation.csv
data/sales_train_validation.csv
data/sell_prices.csv
data/calendar.csv
data/sample_submission.csv

# Intermediate parquet artifacts — regeneratable from notebooks
data/*.parquet

# Executed notebook copies (regenerated by `jupyter nbconvert --execute`)
notebooks/*.executed.ipynb

# Small generated CSVs (model_scorecard, lag_analysis, etc.) ARE committed —
# they're the project's headline results, viewable directly on GitHub.
# Charts in presentation/*.png ARE committed for the same reason.

# Local-only working docs (not for the public repo)
PROMPTS.md
SATURDAY_PLAN.md

# OS
.DS_Store
